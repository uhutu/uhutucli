
machine:
  node:
    version: 6.2.0
deployment:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
  npm:
    tag: /v[0-9]+(\.[0-9]+)*/
        commands:
      - npm publish
